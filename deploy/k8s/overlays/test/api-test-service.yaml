apiVersion: v1
kind: Service
metadata:
  name: rebar-api-test
  namespace: rebar-optimizer
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: http
  selector:
    app: rebar-api-test
  type: ClusterIP

