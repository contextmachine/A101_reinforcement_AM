# Paused at 0: the production worker of the test stack is shut down; tasks are served by the
# alternative solver ScaledJob (rebar-solver-worker-test). Remove the annotation to re-enable.
apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  finalizers:
  - finalizer.keda.sh
  labels:
    scaledobject.keda.sh/name: rebar-worker-test
  name: rebar-worker-test
  namespace: rebar-optimizer
  annotations:
    autoscaling.keda.sh/paused-replicas: '0'
spec:
  advanced:
    horizontalPodAutoscalerConfig:
      behavior:
        scaleDown:
          stabilizationWindowSeconds: 150
    scalingModifiers: {}
  cooldownPeriod: 300
  maxReplicaCount: 12
  minReplicaCount: 0
  pollingInterval: 5
  scaleTargetRef:
    name: rebar-worker-test
  triggers:
  - authenticationRef:
      name: rebar-redis-auth
    metadata:
      activationListLength: '0'
      address: rebar-redis.rebar-optimizer.svc.cluster.local:6379
      listLength: '1'
      listName: rebar:test:jobs:workload
    type: redis
